{
  "mcpServers": {
    "systems": {
      "command": "systems-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "SYSTEMS_URL": "${SYSTEMS_URL:-http://localhost:8080}",
        "SYSTEMS_TOKEN": "${SYSTEMS_TOKEN}"
      }
    }
  }
}
